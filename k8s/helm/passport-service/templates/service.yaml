apiVersion: v1
kind: Service
metadata:
  name: {{ include "passport-service.fullname" . }}
  labels:
    {{- include "passport-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passport-service.selectorLabels" . | nindent 4 }}
